# Required. A Hardcover personal access token, used server-side only.
# hardcover.app > account settings > Hardcover API
#
# Paste it with no "Bearer " in front — the code adds that.
HARDCOVER_TOKEN=
